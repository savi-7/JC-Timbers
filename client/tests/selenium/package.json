{
  "name": "jc-timbers-tests",
  "version": "1.0.0",
  "description": "Playwright tests for JC-Timbers",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug"
  },
  "dependencies": {
    "@playwright/test": "^1.56.1"
  }
}
